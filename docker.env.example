# ===========================================
# IT Project 80 - Environment Configuration
# ===========================================
# 
# INSTRUCTIONS FOR TEAM MEMBERS:
# 1. Copy this file to 'docker.env' in the project root
# 2. Update the values according to your needs
# 3. DO NOT commit docker.env to version control (it's in .gitignore)
#
# Example: cp docker.env.example docker.env

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
DB_HOST=database
DB_PORT=5432
DB_NAME=assignment_mod
DB_USER=postgres
# Change this password for your local environment
DB_PASSWORD=assignment_db_2024

# ===========================================
# BACKEND CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=3000

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
FRONTEND_PORT=80

# ===========================================
# NETWORK CONFIGURATION
# ===========================================
NETWORK_NAME=assignment_network

# ===========================================
# VOLUME CONFIGURATION
# ===========================================
POSTGRES_DATA_VOLUME=assignment_postgres_data

# ===========================================
# SECURITY NOTES
# ===========================================
# - Each team member should use their own DB_PASSWORD
# - For production, use strong, unique passwords
# - Consider using environment-specific configurations
# - Never commit actual credentials to version control
